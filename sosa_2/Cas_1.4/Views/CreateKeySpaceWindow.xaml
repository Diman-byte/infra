<Window x:Class="Cas_1._4.Views.CreateKeySpaceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Создание Keyspace" Height="200" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Keyspace:" Grid.Row="0" />
        <TextBox x:Name="KeyspaceTextBox" Grid.Row="0" Grid.Column="1"/>

        <Button Content="Создать Keyspace" Grid.Row="1" Click="CreateKeyspace_Click" />

        <!-- Добавьте другие элементы управления, если нужно -->
    </Grid>
</Window>
